apiVersion: v1
kind: ConfigMap
metadata:
  name: webapp-config
  namespace: production
  labels:
    app: webapp
    component: config
data:
  APP_ENV: "production"
  APP_VERSION: "1.0.0"
  PORT: "3000"
  LOG_LEVEL: "info"
  MAX_CONNECTIONS: "100"
  ENABLE_METRICS: "true"
  ENABLE_CACHE: "true"